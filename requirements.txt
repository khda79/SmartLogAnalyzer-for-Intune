# SmartLogAnalyzer for Intune — Python dependencies
# Core parsers use Python stdlib; Pillow is used by the GUI for the logo.
# PyInstaller is used for compilation only.

pyinstaller>=6.0
pillow>=10.0
